{
  "contract_version": 1,
  "task": "AF-CLD-024",
  "status": "inventory_and_local_admission_components_only",
  "profile": "development-node-admission-lab-v1",
  "core_dependency": "765bea67f0164a71b24a8e5d042cd4d90c3e7101",
  "inventory_owner": "Core hardware_inventory.collect_inventory",
  "job_authority": "Core scheduler and worker runtime",
  "cloud_responsibility": "Tenant admission and host-profile evidence",
  "qualified_capabilities": [],
  "execution_eligible": false,
  "workspace_policy": "Before collection: lexical rejection, known local drive/mount classification, then parent-by-parent no-link metadata; unknowns fail closed",
  "reported_server": "Unknown target/access; not inventoried or qualified",
  "required_before_dispatch": [
    "Authenticated worker identity and trusted tenant binding",
    "Atomic Core qualification, lifecycle and capacity admission",
    "Exact tenant/run/task/worktree/attempt binding",
    "Tested toolchain versions and bounded workload qualification",
    "Lease expiry, stale completion, drain, quarantine, loss and replacement drills"
  ],
  "separate_acceptance_gates": [
    "Reported server inventory and access",
    "Worker registration and synthetic lab fault evidence",
    "Sandbox qualification",
    "Provider/model execution qualification",
    "Hosted production readiness"
  ],
  "admission_transport": {
    "installation": "Explicit optional router; absent from default creator application",
    "allowed_peer": "Loopback only; deny browser origins and proxy headers",
    "authentication": "Host-provisioned hashed expiring bearer credential bound to Core worker/tenant/version",
    "claim_input": [
      "request_id"
    ],
    "renew_input": [
      "request_id",
      "fencing_token"
    ],
    "request_authority": "Immutable host allowlist of exact Core AdmissionRequest records",
    "scheduler": "Core WorkerAdmissionService and SQLiteStorage, no Cloud lease database",
    "worker_stop_or_result_authority": false,
    "execution_eligible": false
  }
}
