# AI connection guidance

AF-CLD-027 guidance component only. The whole task is incomplete.

Open **Understand AI connection options** from the proposed game team. The link opens a separate tab to preserve the current team review. The Ukrainian guidance page offers the choices from Core's dated provider connection catalogue and displays its explanations and official sources, plus the matching Core eligibility requirements and privacy notes. Chat subscriptions, API accounts and official coding-tool login remain distinct. The unsupported choice offers no connection action.

This is a read-only local operator consumer. It retains the accepted Core dependency 7ec9ff33f1c4e7f2980e4b121d05f3c366acedd1 already used by Cloud; no dependency or database migration is introduced. Core owns catalogue content and eligibility rules. Cloud maps product IDs to the existing informational eligibility routes and calls setup_decision with no approval. It cannot grant access. There is no key field, login-code receiver, endpoint input, connection write, canary, issuer or runtime path. No host approval resolver is installed here. Even a future reviewed setup permission would not itself establish model capability or execution permission.

GET /api/connection-guidance uses the existing Core local HTTP boundary and local tenant scope, returns no-store, rejects query input and exposes no account/private approval data. Unsupported POST routes cannot create a connection. The UI shows all six checks as not run; selecting a product is local display only. It does not call a provider or send the saved idea. Catalogue failure clears displayed options; reload can recover. A newer refresh supersedes earlier responses. Either expired catalogue suspends guidance, with the strict earlier date as the client deadline; expiry is rechecked on selection, visibility and a bounded rescheduling timer.

Tests cover exact Core projection and detached snapshots, both catalogue gates, current/expired dates, no setup/execution authority, authentication/tenant denial, error sanitization, unavailable writes, real Chromium at390px, no secret input or model invocation, expiry and stale-response recovery. These are guidance tests, not actual provider access or bounded coding/review canaries. Full AF-CLD-027 still needs accepted Core evaluator integration, actual independent model evidence, route-specific error recovery and supported protected login/setup. No Ready or full-task acceptance is claimed.
