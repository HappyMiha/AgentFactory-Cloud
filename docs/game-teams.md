# My proposed game team

AF-CLD-009 component; the whole task is not complete.

Open **See my proposed game team** from the first-playable scope page. Five roles explain the Game Director, Designer, Developer, QA/Playtester and Build Engineer responsibilities. The versioned, provider-neutral role pack is 1.0.0, using Core RoleDefinition contracts. Developer and QA have incompatible duties. Their fixed token shares are 15/20/35/20/10 percent of the saved planning allowance, with any rounding remainder assigned to the director. These shares are not active limits or approved spending.

Only after agreeing to a current saved scope can the creator explicitly prepare a missing-capability assessment. This registers immutable role definitions and uses Core WorkforceComposer to record five empty candidate pools with zero paid budget. Core owns the composition records and idempotency. The assessment key binds actor, idea and source revision, current saved scope, mission and role-pack digest. A repeat after restart reuses the same Core record. Concurrent brief/scope edits are serialized while reading the bound snapshot and preparing the assessment; a changed digest requires review again. The original idea and saved scope are never modified.

There is no accepted current-connection resolver installed in this component, so no caller can nominate workers, providers, models, qualification receipts or exceptions. Candidate lists are always empty. This intentionally exercises Core's missing-capability path; it does not qualify its legacy routing path for live Cloud execution. Brand names, stored keys, a previous qualification and caller-provided connection scope are insufficient. Future integration must consume the accepted current-connection/evaluator authority and revalidate independent effective identities after fallback/retry and at execution admission.

The screen states that no team is running. All roles are unassigned, current work is absent, recorded spend is unknown, and approved spend is zero. Start and stop are unavailable, and no corresponding endpoints exist. A disabled stop control is not a claim that a running team can be stopped. The whole task still needs actual current-work/budget reporting, authenticated live start/stop through Core and actual independent coding/review evidence. The preparation button is not an execution or spend approval.

The endpoint is composed into the existing local single-operator Cloud app. Owner-bound brief access, local tenant checks, origin/write-scope enforcement and bounded bodies remain in force. GET displays the current proposal; POST `/api/briefs/{id}/team/assess` accepts only a snapshot digest and strict confirmation. No new Cloud coordination database or scheduler is introduced. The page clears private scope/budget display on failed refresh and supports reload; mutations cannot be requested concurrently from that page.

Core dependency: `7ec9ff33f1c4e7f2980e4b121d05f3c366acedd1`, including the accepted qualification and guidance components. This is a source pin, not live model or engine qualification. Historical upstream-map source baselines remain preserved; the separate component-consumer entry records this narrower usage.

Validation covers role registration, immutable/replay behavior, changed brief/scope, cross-actor/tenant denial, rejected provider/candidate injection, absence of execution records, real Chromium rendering at 390px and failure/reload. Synthetic empty pools cannot satisfy live role capability tests. Actual old-pin database preservation is recorded in the PR alongside exact tests and build results.
